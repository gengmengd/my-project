<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gmd.Mapper.PrincipalMapper">
    <select id="login" resultType="Principal">
    select * from principal where Prinnum = #{prinnum} and Prinpwd = #{prinpwd}
    </select>
</mapper>